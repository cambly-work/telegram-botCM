# CODE: Magnetism Telegram Bot

Этот репозиторий содержит Telegram-бота и вспомогательный REST API, написанный на FastAPI и aiogram. Проект готов к деплою «из коробки» в Docker вместе с PostgreSQL.

## Структура

- `app.py` — основной FastAPI/aiogram-приложение.
- `db.py`, `schema.sql` — инициализация и утилиты работы с PostgreSQL.
- `handlers.py`, `keyboards.py`, `scheduler.py` — логика бота, клавиатуры и планировщик.
- `docker-compose.yml` — оркестрация Docker-сервисов (бот + PostgreSQL).
- `Dockerfile` — образ приложения.
- `.env.example` — пример файла окружения.
- `.dockerignore` — исключения из контекста сборки.

## Быстрый старт в Docker

1. **Склонируйте репозиторий на сервер:**

   ```bash
   git clone https://github.com/<your-org>/telegram-botCM.git
   cd telegram-botCM
   ```

2. **Создайте файл окружения:**

   ```bash
   cp .env.example .env
   ```

   Обязательно укажите боевой `BOT_TOKEN`, `PUBLIC_BASE_URL`, `WEBHOOK_SECRET`, идентификаторы администраторов и другие значения.

3. **Запустите сервисы:**

   ```bash
   docker compose up -d --build
   ```

   По умолчанию FastAPI доступен на `http://localhost:8000`, Postgres — во внутренней сети Compose.

4. **Проверьте работоспособность:**

   ```bash
   curl http://localhost:8000/health
   ```

   Ожидаемый ответ — JSON со статусом `ok`.

## Публичный доступ к вебхукам

Для работы вебхуков Telegram нужен публичный HTTPS-адрес. Приложение поддерживает автоматическую
подстановку URL, выданных **Cloudflare Tunnel (cloudflared)** или **ngrok**.

### Cloudflare Tunnel (cloudflared)

Подходит для VPS без Docker и не требует платного аккаунта. Запустите `cloudflared`, указав локальный
адрес FastAPI и включив метрики:

```bash
cloudflared tunnel --url http://127.0.0.1:8000 --metrics 127.0.0.1:49999
```

В `.env` добавьте:

```dotenv
PUBLIC_BASE_URL=cloudflared
CLOUDFLARED_AUTOFETCH=1
CLOUDFLARED_METRICS_URL=http://127.0.0.1:49999/metrics
```

При необходимости уточните список доменных суффиксов (`CLOUDFLARED_HOSTNAME_SUFFIXES`) или задайте
собственное регулярное выражение (`CLOUDFLARED_URL_REGEX`). В Docker Compose сервис `cloudflared`
запускается автоматически и публикует метрики на `http://cloudflared:49999/metrics`.

### Docker + ngrok

Если нужно вернуться к ngrok (например, для локальной разработки), включите профиль `ngrok`:

```bash
docker compose --profile ngrok up -d --build
```

В `.env` установите:

```dotenv
PUBLIC_BASE_URL=ngrok
NGROK_AUTOFETCH=1
NGROK_API_URL=http://ngrok:4040/api/tunnels
```

Наблюдать за выданным адресом можно командой `docker compose logs -f ngrok` или запросом
`http://localhost:4040/api/tunnels`. Чтобы вернуться к cloudflared, остановите профиль:
`docker compose down ngrok` и запустите `docker compose up -d` без профиля.

## Переменные окружения

| Переменная | Назначение |
| ---------- | ---------- |
| `BOT_TOKEN` | Токен Telegram-бота |
| `ADMIN_IDS` | CSV-список Telegram ID администраторов |
| `PUBLIC_BASE_URL` | Публичный URL, по которому Telegram обращается к вебхуку. Значения `cloudflared` или `ngrok` включают автоподстановку |
| `WEBHOOK_SECRET` | Секрет для ручного управления вебхуком и админ-эндпоинтов |
| `CLOUDFLARED_AUTOFETCH` | Если `1/true` — автоматически подтянуть `PUBLIC_BASE_URL` из cloudflared |
| `CLOUDFLARED_METRICS_URL` | Endpoint метрик cloudflared (например, `http://cloudflared:49999/metrics`) |
| `CLOUDFLARED_HOSTNAME_SUFFIXES` | Список суффиксов доменов (через запятую), по которым ищется URL cloudflared |
| `CLOUDFLARED_URL_REGEX` | Пользовательский RegExp для поиска URL cloudflared (опционально) |
| `NGROK_AUTOFETCH` | Если `1/true` — автоматически подтянуть `PUBLIC_BASE_URL` из ngrok |
| `NGROK_API_URL` | Endpoint ngrok, возвращающий список туннелей (например, `http://ngrok:4040/api/tunnels`) |
| `NGROK_API_TOKEN` | Токен API ngrok (нужен для облачного API `https://api.ngrok.com`) |
| `NGROK_TUNNEL_NAME` | Фильтр по имени/URL туннеля ngrok, если их несколько |
| `DB_DSN` | Строка подключения к PostgreSQL (для Docker — `postgres`) |
| `BOT_TIMEZONE` | Таймзона планировщика уроков |
| `TIME_SEND_LESSONS` | Локальное время запуска рассылки уроков |
| `WELCOME_POST_URL` | Ссылка на приветственный пост клуба |
| `TEST_FORM_URL` | Ссылка на тест для определения уровня |
| `SUPPORT_CONTACT` | Контакт службы поддержки |
| `CLUB_CHAT_ID` | Числовой chat_id клубного чата (бот должен быть админом) |
| `AT_WEBHOOK_SHARED_SECRET` | Секрет для вебхуков Anti-training (опционально) |
| `YOOMONEY_CHECKOUT_URL` | Ссылка на страницу оплаты YooMoney |
| `YOOMONEY_WEBHOOK_SECRET` | Секрет для подписи вебхука YooMoney |

## Разработка без Docker

1. Установите зависимости:

   ```bash
   python -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   ```

2. Поднимите PostgreSQL локально или используйте Docker-контейнер.
3. Настройте `.env` (см. пример).
4. Запустите приложение:

   ```bash
   uvicorn app:app --reload
   ```

## Тестирование

Для запуска юнит-тестов установите `PYTHONPATH` и выполните:

```bash
PYTHONPATH=. pytest
```

## Ручное применение миграции напоминаний

Для продакшн-базы важно убедиться, что у таблицы `form_sessions` есть поля
`last_reminder_at` и `reminder_count`. Их добавляет скрипт
`migrations/20240703_form_sessions_form_slug.sql`. Минимальный набор команд для
ручного прогона:

```sql
ALTER TABLE form_sessions
    ADD COLUMN IF NOT EXISTS last_reminder_at TIMESTAMPTZ,
    ADD COLUMN IF NOT EXISTS reminder_count INT DEFAULT 0;

UPDATE form_sessions
SET reminder_count = COALESCE(reminder_count, 0);
```

Проверить наличие колонок можно запросом:

```sql
SELECT column_name
FROM information_schema.columns
WHERE table_name = 'form_sessions';
```

## Прогресс уроков и ручная корректировка

Статусы обучения сохраняются в двух таблицах:

- `funnel_progress` — фиксирует выдачу уроков, статус домашнего задания (`submitted`, `pending`, `skipped`) и текст ответа.
- `lesson_feedback` — хранит обратную связь по урокам и используется как признак завершения, если статуса в `funnel_progress` нет (например, после импорта).

Бот комбинирует обе таблицы, чтобы построить блок «Мой прогресс»: статус урока определяется по `hw_status`, но при наличии отзывов урок также помечается завершённым. В профиле и в разделе обучения дополнительно отображается статус доступа («Без подписки», «Активный доступ», «Доступ истёк») и дата окончания в формате `ДД.ММ.ГГГГ`.

### Админ-панель

- В карточке пользователя появилась кнопка «📈 Обновить прогресс». Формат строк для ручной правки: `lesson=1 status=submitted feedback=excellent,good delivered=2024-08-01 reset=0`. Поддерживаются поля `lesson`, `status`, `feedback`, `delivered`, `opened`, `answer`, `reset`.

### Админский API

- `GET /admin/progress?secret=...&user_id=123` — возвращает текущий прогресс (уроки, статусы, отзывы).
- `POST /admin/progress/sync?secret=...` — принимает JSON с массивом `lessons` и синхронизирует статусы. Пустой `feedback` очищает отзывы, `reset=true` удаляет урок из истории. Поле `actor_id` логирует действие в `admin_log`.

Подробнее структура описана в [`docs/progress_data.md`](docs/progress_data.md).

## Обновление и деплой

```bash
git pull
docker compose pull
docker compose up -d --build
```

Docker перезапустит сервисы с обновлённым кодом. Логи приложения доступны через `docker compose logs -f bot`.

## Настройка YooMoney

- Укажите `YOOMONEY_CHECKOUT_URL` в `.env`, чтобы раздел оплаты в боте показывал актуальную ссылку на форму YooMoney.
- Секрет `YOOMONEY_WEBHOOK_SECRET` (если задан) используется для проверки подписи вебхука. YooMoney должна отправлять заголовок `X-YooMoney-Signature` с HMAC-SHA256 тела запроса.
- Адрес обработчика: `POST <PUBLIC_BASE_URL>/webhooks/yoomoney`. Убедитесь, что в личном кабинете YooMoney настроен этот URL.
- После успешной оплаты бот активирует доступ пользователю, отправляет подтверждение и уведомляет администраторов. При отказе платежа доступ блокируется и админы получают предупреждение.

## Управление окном оплаты и текстами раздела «Пройти тест»

- Статус «Окно оплаты открыто/закрыто» хранится в записи `settings.payments_open` таблицы `content`. Бот сначала читает это значение из базы, а при отсутствии — использует дефолт из `content.yaml` (`true`). Если в БД лежит `false`, бот показывает кнопку «🔒 Оплата» и текст о закрытом окне.【F:handlers.py†L646-L690】【F:handlers.py†L1875-L1894】

## Навигация по меню бота

### Главное меню

- «ℹ️ Клуб» → показывает описание клуба и открывает подменю с материалами, обучением, оплатой и FAQ.【F:keyboards.py†L180-L215】【F:handlers/menu.py†L145-L212】
- «📚 Библиотека» → подборки постов и материалов, которые наполняются через админку.【F:keyboards.py†L186-L198】【F:handlers/menu.py†L258-L278】
- «Пройти тест» → запуск теста и переход к анкете.【F:keyboards.py†L189-L198】【F:handlers/__init__.py†L8086-L8128】
- «Забрать гайд» → выдаёт ссылку на гайд из контента админки.【F:keyboards.py†L189-L198】【F:handlers/menu.py†L235-L255】
- «Записаться на разбор» → оформление заявки на персональный разбор.【F:keyboards.py†L194-L198】【F:handlers/__init__.py†L7960-L8058】
- «Окно в Магнетизм» → переход к форме «Окно в Магнетизм».【F:keyboards.py†L194-L198】【F:handlers/menu.py†L400-L418】
- «🆘 Поддержка» → быстрые контакты службы поддержки (доступно всем пользователям).【F:keyboards.py†L197-L205】【F:handlers/__init__.py†L7912-L7919】
- «⚙️ Админка» отображается только администраторам и открывает административные разделы.【F:keyboards.py†L200-L205】【F:keyboards.py†L344-L402】

### Раздел «Материалы»

- «Материалы недели» — карточка недели с раздачей ссылок и проверкой доступа.
- «Каталог материалов» раскрывает подменю с подкастами, практиками, челленджами и архивом. Вложенные подпункты отмечаются ▶️ при наличии дочерних элементов и 🔒 при закрытом доступе.【F:keyboards.py†L195-L236】【F:content.yaml†L160-L215】
- «Расписание» показывает сетку эфиров, если раздел включён в настройках (иначе отображается вариант с замком).【F:keyboards.py†L203-L229】【F:content.yaml†L120-L158】
- Кнопки «↩️ Назад к каталогу» и «🏠 В главное меню» доступны во всех подуровнях для возврата навигации.【F:keyboards.py†L207-L229】

### Административная панель

- Главное меню админки сгруппировано по четырём категориям: «👥 Пользователи», «🧾 Контент», «📣 Коммуникации» и «🛠️ Служебное». Кнопка «⬅️ В админку» всегда возвращает к этому списку.【F:keyboards.py†L352-L404】【F:handlers.py†L5784-L5820】
- Категория «👥 Пользователи» объединяет сегменты участниц, ручное подтверждение оплат и управление ключами доступа.【F:keyboards.py†L367-L374】【F:handlers.py†L5822-L5851】
- Категория «🧾 Контент» ведёт к редактору текстов, управлению материалами и настройке логики бота и онбординга.【F:keyboards.py†L377-L384】【F:handlers.py†L5854-L5870】
- Категория «📣 Коммуникации» содержит рассылки и календарь событий/напоминаний.【F:keyboards.py†L387-L393】【F:handlers.py†L5873-L5889】
- Категория «🛠️ Служебное» включает статистику, настройки и диагностику для проверки интеграций.【F:keyboards.py†L396-L403】【F:handlers.py†L5892-L5908】

## Редактирование контента через админку

Админ-панель содержит отдельный раздел «📝 Контент» с кнопками «📚 Меню», «⬇️ Экспорт», «⬆️ Импорт», «🏷 Плейсхолдеры» и т.д. Основные шаги и правила работы:

1. Откройте раздел «⚙️ Админка» → «📝 Контент». Клавиатура подскажет доступные действия и сохранённые шаблоны.【F:handlers.py†L9730-L9779】【F:keyboards.py†L581-L643】
2. При экспорте бот отправляет YAML и JSON файлы с текущими значениями ключей. Используйте их как основу для правок, чтобы сохранить структуру ключей.【F:handlers.py†L9763-L9837】
3. Для импорта допускаются только пары `key: value`, где ключ соответствует паттерну `^[A-Za-z0-9_.-]{3,}$`. Некорректные ключи и пустые значения будут отвергнуты на этапе загрузки.【F:handlers.py†L1829-L1831】【F:handlers.py†L9856-L9867】
4. Допустимые теги: `*bold*`, `_italic_`, `__underline__`, `` `code` `` и `[текст](https://url)` автоматически преобразуются в HTML. Также разрешены чистые HTML-теги `<b>`, `<i>`, `<u>`, `<code>`, `<a href="…">`. Чтобы вывести спецсимволы без форматирования, экранируйте их обратным слешом.【F:handlers.py†L9738-L9761】【F:handlers.py†L430-L470】
5. Плейсхолдеры обязаны сохраняться в точности: для каждого ключа бот проверяет наличие обязательных токенов. Например, `menu.start` и `menu.registration_complete` требуют `{name}`, `menu.pay` — `{checkout_url}`, `menu.support` — `{support}`. При отсутствии обязательных плейсхолдеров бот не примет текст.【F:handlers.py†L1672-L1713】【F:handlers.py†L1796-L1820】【F:handlers.py†L10143-L10167】
6. Подсказки к ключам (о назначении текстов) доступны прямо в админке: бот отображает описание и актуальную версию с авторами и временем изменения.【F:handlers.py†L1760-L1779】【F:handlers.py†L10026-L10143】

При редактировании через чат админки используйте предпросмотр, чтобы убедиться, что плейсхолдеры и ссылки подставляются корректно. Все изменения логируются через `admin_log` и доступны в истории версий.【F:handlers.py†L949-L10547】

## Шаблоны рассылок и чек-лист подготовки

Раздел «📣 Рассылки» админки объединяет настройки сегментов, историю кампаний и шаблоны сообщений:

- Готовые ключи для рассылок перечислены в меню «🛠 Подсказки админки», включая `forms.reminder_template` и описания основного меню рассылок.【F:handlers.py†L1662-L1677】
- Каждый шаблон может использовать динамические плейсхолдеры `{name}`, `{support}` и др. — бот подсвечивает, какие токены доступны и какие значения будут подставлены на примере (Алиса, контакты поддержки).【F:handlers.py†L1159-L1193】【F:handlers.py†L1779-L1820】
- Перед запуском рассылки проверьте чек-лист:
  1. Обновлены тексты в `content.yaml` или через админку и все обязательные плейсхолдеры на месте.
  2. Сегмент выбран корректно (лиды, активные, завершившие и т.д.) — ограничения сегментов описаны в боте.【F:handlers.py†L1662-L1677】【F:keyboards.py†L312-L404】
  3. CTA-кнопки заполнены валидными URL или deep-link payload; бот очищает пустые кнопки автоматически.【F:handlers.py†L10803-L10872】【F:handlers.py†L10895-L10960】
  4. Проверен предпросмотр и тестовая отправка администратору.
  5. Зафиксировано окно отправки в планировщике, если сообщение должно уйти по расписанию (`scheduler.job_daily_lessons`, сегменты форм).【F:scheduler.py†L1-L120】【F:handlers.py†L5550-L5794】

## Формы, анкеты и импорт домашних заданий

### Отслеживание форм

- Бот нормализует идентификаторы форм через `resolve_form_slug` и сохраняет сессии в таблице `form_sessions`. Статус «начата» и «завершена» фиксируется при переходах по ссылкам и ручной отметке через `/form_done <slug>`.【F:handlers.py†L347-L433】【F:handlers.py†L6632-L6815】
- Шаблон напоминаний по незавершённым анкетам хранится в ключе `forms.reminder_template` и требует плейсхолдер `{form_label}`. Используйте его для рассылок, чтобы бот подставил читаемое название формы.【F:handlers.py†L1662-L1677】【F:handlers.py†L1717-L1724】
- В административной статистике доступен отдельный разрез по формам: количество начатых/завершённых, даты последней активности и активные напоминания.【F:handlers.py†L2173-L2529】【F:keyboards.py†L378-L404】

### Импорт и экспорт домашних заданий

- Экспорт CSV доступен по `GET /admin/homework/export?secret=...&segment=all|lead_funnel|member_active|member_expired`. Файл содержит пользователей, четыре строки по каждому уроку и текущие статусы ДЗ.【F:app.py†L741-L828】
- Для импорта используйте `POST /admin/homework/import?secret=...` с телом `{ "csv": "...", "delimiter": ",", "dry_run": false, "actor_id": 123 }`. Допускается импорт по `user_id`, `tg_user_id` или контактам (email, phone).【F:app.py†L830-L907】
- Поддерживаются колонки `lesson`, `hw_status`, `delivered_at`, `opened_at`, `hw_answer`, `reset`. Минимально укажите `lesson` (1–4) и один из идентификаторов пользователя (`user_id`, `tg_user_id` или контакты). Некорректные значения бот поясняет в ответе API; при `dry_run=true` данные только валидируются.【F:app.py†L830-L972】
- После успешного импорта бот пересобирает прогресс через `sync_user_progress`, логирует действие и уведомляет администраторов при ошибках. Ответ включает количество обработанных строк и пользователей.【F:app.py†L930-L972】

Перед загрузкой убедитесь, что даты (`delivered_at`, `opened_at`) указаны в ISO 8601, а тексты ответов ДЗ без переносов строки — экспорт заменяет их пробелами, чтобы структура CSV не ломалась.【F:app.py†L780-L828】【F:app.py†L900-L948】
- Переключить статус можно в админке: `⚙️ Админка → 💳 Оплаты → 🔓 Открыть окно оплат/🔒 Закрыть окно оплат`. Кнопка вызывает `set_bool_setting("payments_open", ...)`, которое обновляет запись в таблице `content` и сразу сбрасывает кеш — перезапуск бота не требуется.【F:handlers.py†L5620-L5628】【F:handlers.py†L688-L715】
- Тексты цепочки «Пройти тест» читаются через ключи `menu.test`, `menu.test_intro`, `menu.test_birthdate_prompt`, `menu.test_birthdate_invalid`, `menu.test_name_prompt`, `menu.test_name_invalid`, `menu.test_thanks`. Для каждого ключа бот сначала пытается найти значение в таблице `content`, и только если его нет — берёт fallback из `content.yaml`. Поэтому, чтобы отображался текст, сохранённый через админку, убедитесь, что запись с нужным `key` есть в таблице `content` (её можно добавить/отредактировать через раздел «Контент» админки).【F:handlers.py†L646-L689】【F:handlers.py†L2110-L2144】【F:handlers.py†L3783-L3894】

## Резервное копирование данных

- Данные PostgreSQL хранятся в volume `postgres_data`.
- Для бэкапа используйте `docker run --rm --volumes-from code-magnetism-db postgres:15-alpine pg_dump -U magnet magnetism > backup.sql`.

